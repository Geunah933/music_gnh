import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Smoke test to verify the app builds without errors.
    // Note: This requires Hive to be initialized first.
  });
}
